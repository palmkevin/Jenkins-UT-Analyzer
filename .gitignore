# Secrets / local config — never commit
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Docker / data
pgdata/

# Parallel dev worktrees (live inside the bind-mounted workspace; share the one .git)
.worktrees/

# Editor / OS
.DS_Store
